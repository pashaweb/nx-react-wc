import { render } from '@testing-library/react';

import SvgStageReact from './svg-stage-react';

describe('SvgStageReact', () => {
  // it('should render successfully', () => {
  //   const { baseElement } = render(<SvgStageReact />);
  //   expect(baseElement).toBeTruthy();
  // });
});
